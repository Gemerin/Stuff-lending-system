package model.menu;

/**
 * Represents the main menu for item-related actions in the application.
 */
public class ItemMainMenu {
  /**
   * Enumeration of choices available in the item main menu.
   */
  public enum ItemMainMenuChoice {
    SELECT_ITEM,
    RETURN,
    INVALID;
  }
}
